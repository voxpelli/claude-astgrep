## Feature Requests

- **LSP `extensionToLanguage` cannot be dynamic, computed, or overridden per project** (2026-07-11) — A plugin's `lspServers.<id>.extensionToLanguage` must be a **static** map. There are no wildcards or globs, no way to point `lspServers` at an executable whose stdout is the config, no lifecycle hook that runs before `lspServers` is parsed, and no way for a project's `.claude/settings.json` to override a plugin-provided server's extension map. Only three interpolations exist (`${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_PLUGIN_DATA}`, `${CLAUDE_PROJECT_DIR}`) and they cannot appear in the map's keys.
  This is a hard bottleneck for **polyglot** language servers. ast-grep lints 28 languages, but *which* ones matter is a property of the individual project — it is exactly the set of `language:` values in that project's own `.ast-grep/rules/*.yml`, and it is trivially derivable from them. There is no way to express that. Worse, a static map can never even be **complete**: ast-grep's `customLanguages` lets a project register a tree-sitter grammar under **arbitrary** extensions that no plugin-side map can anticipate. Upstream's own VSCode extension sidesteps this entirely by claiming every file (`documentSelector: [{ scheme: 'file', language: '*' }]`) and gating on the config file's presence instead — an option Claude Code does not offer.
  Ownership: upstream · Workaround: partial — ship a conservative static map and accept that it is wrong for somebody.

- **No LSP extension-collision precedence, and no documented fallthrough** (2026-07-11) — When two enabled LSP servers declare the same extension in `extensionToLanguage`, "the first server registered handles files with that extension and the others never start". But **registration order is undocumented** (alphabetical? install order? settings order?), there is **no priority/ordering field**, and there is **no way to run both** servers on one extension. So a lint-shaped server and a types-shaped server cannot coexist on `.ts` at all, and which one wins is unpredictable.
  The load-bearing undocumented case: **if the first claimer dies during `initialize`, is the extension freed for the next claimer, or starved?** `ast-grep lsp` exits during initialize in any project lacking an `sgconfig.yml` — i.e. most projects. If Claude Code falls through, a polyglot lint server could safely claim broadly and simply be inert where it has no config. If it starves, the same claim silently disables `pyright-lsp`/`rust-analyzer-lsp` in every repo. **The answer completely determines a safe plugin design, and it is not documented.** We assumed the pessimistic reading and claim only what we lint.
  Ownership: upstream · Workaround: partial — never claim an extension an official language server owns; enforced by a check in this repo.

## Bugs

- **LSP client does not honour a server's dynamic `workspace/didChangeWatchedFiles` registration** (2026-07-11) \[degraded\] — A language server that asks the *client* to watch files on its behalf — via a `client/registerCapability` request for `workspace/didChangeWatchedFiles`, the standard LSP mechanism — is **never notified**. Claude Code appears not to implement file watching for LSP. Nothing errors; the registration is simply ignored.
  ast-grep's server relies on exactly this to hot-reload its rule files (it has no watcher of its own). Result: **rule edits are silently ignored until Claude Code is restarted.** The server serves its startup rule set forever.
  **The symptom is deceptive, and that is the real cost.** Document sync keeps working *perfectly*: edit a source file and the server re-analyses it instantly, correct line numbers and all. Only the rule-reload channel is dead. So the plugin looks healthy while being half deaf, and a user editing a rule concludes their rule is wrong rather than unloaded. Verified by changing a rule's `message:`, waiting 25s, touching the file, and re-triggering: the server reported the **old** message against **freshly-analysed** code (the diagnostic's line number tracked the edit; its text did not). A fresh `ast-grep scan` process read the new rule immediately — so: live server stale, new process current. Not lag.
  **MEASURED (2026-07-11), and it narrows the blast radius — Claude Code does not advertise the capability at all.** Driving a real session with `claude --plugin-dir … --debug-file` and logging the client's `initialize` from inside the server: `workspace.didChangeWatchedFiles.dynamicRegistration = undefined`. No public dump of Claude Code's advertised capabilities existed before this.
  That is, on its own, *correct* behaviour: a client that cannot watch files should not claim it can. It means **`rust-analyzer`, `pyright`, `gopls` and `clangd` are NOT broken here** — they respect the capability, decline to register, and fall back to their own watchers. (An earlier revision of this entry speculated they were silently broken. They are not. Recorded because a wrong note compounds.)
  The bug is therefore narrower than it first looked, and it only bites servers that register **without checking the capability** — which is itself a spec violation on their side. ast-grep does this and merely goes stale (it uses `tower-lsp`, which fires the request without blocking). `csharp-lsp` and `elixir-lsp` do it too and **block on the reply and hang forever** — broken out of the box, in Anthropic's own marketplace.
  So there are two defensible reads: *"the client is fine; those servers are buggy"*, or *"a client that answers a core LSP request with `-32601` is not a conforming client."* Both are true. The user-visible fact is that the feature — client-side file watching — is simply **absent**, which is what the Feature Request below asks for.
  **Both upstream issues are closed as NOT_PLANNED** (`anthropics/claude-code#32595` and its re-file `#52693`), which is what turned the workaround below from a stopgap into the only path. A third-party byte-log in `anthropics/claude-plugins-official#1359` caught `workspace/configuration` and `window/workDoneProgress/create` getting the same `-32601` treatment, so this is a general gap in the LSP client, not a one-off.
  Severity: degraded · Ownership: upstream · Workaround: **full** — `bin/lsp-shim.mjs` in this repo (shipped in 0.4.0). A stdio shim answers the `registerCapability` request on the client's behalf, reads the watcher globs out of the registration payload, watches them, and injects the `workspace/didChangeWatchedFiles` the client never sends. Server-agnostic: it spawns `argv[0]` and watches whatever globs the *server* asked for, so the same shim would unhang `csharp-lsp` and `elixir-lsp`. Proven by `verify-lsp-reload.mjs`, which asserts the bare server ignores an on-disk rule change and the shimmed one does not.

## Upstream Opportunities

- **A stdio shim that answers `client/registerCapability` on the client's behalf** (2026-07-11) — Exists only because the client will not. **Explicitly a stopgap, not a product** — see the retirement trigger below. Recorded here because it is a working existence proof, not because it wants to be a plugin.
  Its one genuinely general use is that the same unanswered request makes `csharp-lsp` and `elixir-lsp` **hang outright** rather than merely go stale, so this code would unbreak two servers in Anthropic's own marketplace. If anyone wants to argue #32595 back open, a shim demonstrates the mechanism works and costs upstream nothing to verify.
  **Retirement trigger: the ast-grep self-watch fallback** (see `UPSTREAM-brew--ast-grep.md`). That fix is entirely server-side, needs nothing from Anthropic, and makes this shim dead code for our purposes. It is the durable fix; this is the bridge to it. **Not filed — tracked here only.**
  Source: `bin/lsp-shim.mjs`, `bin/glob-watch.mjs`, `bin/lsp-framing.mjs` (this repo, 0.4.0) · Merge readiness: proof-of-concept
  Ownership: us · Workaround: full — shipped in this plugin, pending the upstream fix that would retire it.
